// src/utils/hasAccessToPanel.ts

import { hasAccessToPanel, hasMinimumProfileLevel, UserProfile, PanelType } from '@/config/profileConfig';

export { hasAccessToPanel, hasMinimumProfileLevel };
export type { UserProfile, PanelType };